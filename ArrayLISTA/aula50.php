<?php
    //ARRAY é um conjunto de elementos contidos numa estrutra onde cada elemnto possui uma chave que o identifica.

    //Importante ressaltar que o PHP utiliza o nome de Array, porém, o funcionamento é totalmente diferente do que encontramos nas linguagens tradicionais como C, C++, Pascal. A principal diferença está no fato de que podemos adicionar, alterar e remover elementos da estrutura muito facilmente.

    //Os elementos de um ARRAY não possui um tipo. Ou seja, o primeiro elemento pode ser um int, float, string, ... Isto é, não existe uma norma de mesmos tipos no array.

    $a = array(1,2,3);
    print_r($a)


?>