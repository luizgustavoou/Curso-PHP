<?php
    //Classe é o projeto de um objeto. Na programação orientada a objetos a classe é a estrutura que irá conter os membros, isto é, os atributos (variáveis) e as funções. Assim, temos que as classes são as principais estruturas e por isso, vamos iniciar o nosso estudo com a definição do que são classes e em seguida, faremos um exemplo onde iremos programar nossa Primeira Classe em PHP.
    
    //Existem algumas nomenclaturas que precisamos saber, por exemplo: 

    //Classe: projeto de objeto/instância

    //Instância: é a execução de uma classe.

    //Objeto: é a execução de uma classe.

    //Herança: capacidade de herdar as características de outra classe 

    //Polimorfismo: capacidade de ter funções com mesmo nome e assinatura, porém, com comportamentos diferentes.

    //Membro de classe: pode ser uma função, ou então, uma propriedade (variável).

    //Propriedade: característica do objeto e na programação, é representando por uma variável.

    //Superclasse: a classe pai; classe que esta sendo estendida.

    //Subclasse: nome da classe que está implementando outra.

    //Classe-pai: classe utilizada como base para a criação de outras.

    //Evento: algo que ocorreu dentro de uma classe e disparou uma funcionalidade.

    #PRIMEIRA CLASSE EM PHP

    class Pessoa{
        
    }


?>